if(NOT DEFINED ENV{ESPHOME_BOARD})
    set(ESPHOME_BOARD esp32-c6-devkitc-1)
endif()

# We expect hcp2-lp.bin to be present in the component directory
# It is generated by the Rust build process.
target_add_binary_data(${COMPONENT_LIB} "hcp2-lp.bin" BINARY)
